config:
  target: "http://localhost:5000/"
  phases:
    - duration: 60  # Test duration in seconds
      arrivalRate: 200  # New users per second

scenarios:
  - flow:
      - get:
          url: "/"
